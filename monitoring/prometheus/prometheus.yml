global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:

  - job_name: "sqlserver"

    metrics_path: /metrics

    static_configs:
      - targets:
          - sql-exporter:9399